# Ajustes de pnpm v11 (los settings ya no van en package.json).
# Aprobamos explicitamente el build script de esbuild (lo usa Vite).
allowBuilds:
  esbuild: true
patchedDependencies:
  recharts@3.9.1: patches/recharts@3.9.1.patch
